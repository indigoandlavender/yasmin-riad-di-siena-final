"use client"; import {useState} from "react"; export default function MessageComposer({guestId}:{guestId:string}){const[text,setText]=useState(""); async function send(){await fetch("/api/send",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({guestId,text})}); setText(""); alert("Message sent");} return(<div><textarea value={text} onChange={e=>setText(e.target.value)} /><button onClick={send}>Send as Yasmin</button></div>);}